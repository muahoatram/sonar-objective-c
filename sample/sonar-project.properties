##########################
# Required configuration #
##########################

sonar.projectKey=com.semweb.GreatReader
sonar.projectName=GreatReader
sonar.projectVersion=1.0
sonar.language=objc
 
# Project description
sonar.projectDescription=A "great" PDF reader for iOS
 
sonar.c.file.suffixes=
sonar.objc.file.suffixes=.h,.m

# Encoding of the source files
sonar.sourceEncoding=UTF-8

# The build-wrapper output dir
sonar.cfamily.build-wrapper-output=DerivedData/compilation-database

# The coverage report generated by llvm-cov
sonar.cfamily.llvm-cov.reportPath=DerivedData/coverage.txt

# Path to source directories 
sonar.sources=GreatReader
# Path to test directories (comment if no test)
sonar.tests=GreatReaderTests

# Coverage type to expect from project
# can be 'legacy' (pre Xcode 7) or 'profdata'
# If not set : defaults to profdata
#sonar.objectivec.coverageType=profdata

# Destination Simulator to run tests
# As string expected in destination argument of xcodebuild command
# Example = sonar.swift.simulator=platform=iOS Simulator,name=iPhone 6,OS=9.2
sonar.objectivec.simulator=platform=iOS Simulator,name=iPhone 8,OS=latest

 
# Xcode project configuration (.xcodeproj or .xcworkspace)
# -> If you have a project: configure only sonar.objectivec.project
# -> If you have a workspace: configure sonar.objectivec.workspace and sonar.objectivec.project
# and use the later to specify which project(s) to include in the analysis (comma separated list)
sonar.objectivec.project=GreatReader.xcodeproj 
sonar.objectivec.workspace=GreatReader.xcworkspace

# Scheme to build your application
sonar.objectivec.appScheme=GreatReader
# Scheme to build and run your tests (comment following line of you don't have any tests)
sonar.objectivec.testScheme=GreatReader
 
##########################
# Optional configuration #
##########################

# Encoding of the source code
sonar.sourceEncoding=UTF-8

# Cobertura report generated by run-sonar.sh is stored in sonar-reports/coverage.xml
# Change it only if you generate the file on your own
sonar.objectivec.coverage.reportPattern=sonar-reports/coverage*.xml

# OCLint report generated by run-sonar.sh is stored in sonar-reports/oclint.xml
# Change it only if you generate the file on your own
sonar.objectivec.oclint.report=sonar-reports/analysis-oclint.xml

# FauxPas report generated by run-sonar.sh is stored in sonar-reports/fauxpas.json
# Change it only if you generate the file on your own
sonar.objectivec.fauxpas.report=sonar-reports/fauxpas.json

# Paths to exclude from coverage report (tests, 3rd party libraries etc.)
# sonar.objectivec.excludedPathsFromCoverage=pattern1,pattern2
sonar.objectivec.excludedPathsFromCoverage=.*Tests.*,.*Specs.*
# Path to build directory if specify config. Slather will using this path to lookup coverage and binary file.
sonar.objectivec.buildDir=Build

# Project SCM settings
# sonar.scm.enabled=true
# sonar.scm.url=scm:git:https://...

